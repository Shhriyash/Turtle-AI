# Python
__pycache__/
*.py[cod]
*.pyd
*.so
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / virtualenv
.env
.env.*
*.env
*.env.*
!.env.example
venv/
.venv/
env/
ENV/
.venv.bak/
venv.bak/
**/venv/
**/.venv/
**/env/
**/ENV/

# Markdown (keep only root README.md)
**/*.md
!README.md
# ...but tool-contract prompts are load-bearing: _load_tool_contract() reads
# them at runtime as each tool's description, and tests assert they exist. They
# must be tracked, or a fresh clone (incl. CI) degrades every tool to
# "Tool: {name}" and the contract tests fail.
!core/system_prompts/tools/
!core/system_prompts/tools/*.md

# IDE / OS
.vscode/
.idea/
.DS_Store

# Personal notes / vaults (dev tooling, not part of the app)
turtle_vault/
.obsidian/

# Runtime / data
.temp/
.gradio/
temp_audio/
temp_streaming/
output/
data/
rag_data/
vector_storage/
*.log
tempCodeRunnerFile.py

# Database files
*.db
*.sqlite
*.sqlite3
*.sql

# Secrets / keys
credentials.json
key.txt
*serviceaccount*.json
*service-account*.json
*firebase-adminsdk*.json
google-services.json
GoogleService-Info.plist
*.pem
*.p12
*.pfx
id_rsa*
!*.example
.claude/
